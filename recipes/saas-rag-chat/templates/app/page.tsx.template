import Link from "next/link";

export default function HomePage() {
  return (
    <main style={{ maxWidth: 720, margin: "4rem auto", padding: "0 1.5rem" }}>
      <h1>{{displayName}}</h1>
      <p>
        Ask questions over your documents. Powered by RAG with hybrid search,
        citation validation, and safe-link rewriting.
      </p>
      <p>
        Forged from <code>{{recipeName}}@{{recipeVersion}}</code> on{" "}
        <time dateTime="{{forgedAt}}">{{forgedAt}}</time>.
      </p>
      <p>
        <Link href="/login">Sign in</Link> to start chatting.
      </p>
    </main>
  );
}
