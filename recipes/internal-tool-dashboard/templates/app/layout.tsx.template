import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{displayName}} — Admin",
  description: "Internal admin tool. Forged from {{recipeName}}@{{recipeVersion}}.",
  robots: { index: false, follow: false }, // never indexed
};

// Recipe ships a minimal layout. Add /dashboard, /audit, sign-out, etc. as
// you build out the tool — typedRoutes will catch missing pages at build time.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="{{defaultLocale | default:"en"}}">
      <body style={{ fontFamily: "system-ui, sans-serif", margin: 0 }}>
        <header
          style={{
            padding: "0.75rem 1.5rem",
            background: "#0f172a",
            color: "#f1f5f9",
          }}
        >
          <strong>{{displayName}}</strong>
        </header>
        <main style={{ padding: "2rem 1.5rem", maxWidth: 1080, margin: "0 auto" }}>
          {children}
        </main>
      </body>
    </html>
  );
}
