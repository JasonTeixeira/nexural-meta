import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{displayName}} - Admin",
  description: "Internal admin tool. Forged from {{recipeName}}@{{recipeVersion}}.",
  robots: { index: false, follow: false },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="{{defaultLocale}}">
      <body
        style={{
          fontFamily: "Aptos, Segoe UI, sans-serif",
          margin: 0,
          background: "#f6f5f1",
          color: "#171717",
        }}
      >
        <header
          style={{
            borderBottom: "1px solid #d8d2c4",
            background: "#111111",
            color: "#f8f5ed",
          }}
        >
          <div
            style={{
              alignItems: "center",
              display: "flex",
              justifyContent: "space-between",
              margin: "0 auto",
              maxWidth: 1180,
              padding: "0.85rem 1.25rem",
            }}
          >
            <strong>{{ displayName }}</strong>
            <span style={{ color: "#d8d2c4", fontSize: "0.85rem" }}>
              {{ recipeName }}@{{ recipeVersion }}
            </span>
          </div>
        </header>
        <main style={{ margin: "0 auto", maxWidth: 1180, padding: "1.5rem 1.25rem" }}>
          {children}
        </main>
      </body>
    </html>
  );
}
