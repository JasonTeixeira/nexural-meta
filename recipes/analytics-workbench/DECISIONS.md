# Decisions

- Keep analytics tenant-scoped by default.
- Treat metric definitions as governed records so dashboards are reproducible.
- Export jobs are explicit records instead of hidden background side effects.
