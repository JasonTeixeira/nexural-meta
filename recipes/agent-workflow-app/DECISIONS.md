# Decisions

- Extend the SaaS baseline directly so app generation keeps the same deployment and auth path as the proven recipes.
- Require human approval by default because tool execution can mutate external systems.
- Persist workflow runs, steps, tool calls, and approvals as separate records for auditability.
