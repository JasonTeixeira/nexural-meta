# @nexural/federation-server

## 1.0.0

- Initial release. Phase 11.x daily-ops payoff.
- Single MCP stdio binary exposing federation_ask + federation_list_sources tools.
- Wires the @nexural/ask-engine FTS5 index over an MCP transport so editor agents can query the federation in one call.
